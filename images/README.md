# images フォルダ

ここに MAMC の商品画像を入れてください。

- 形式: jpg / jpeg / png / gif / webp
- ファイル名は `products.csv` の `image` 列に書いた名前と合わせてください。
- `image` 列が空欄のときは、`id` で始まるファイル名の画像を自動で探します。
  例）id が `mamc-tee-01` なら `mamc-tee-01.jpg` を自動で使います。
- 5MB を超える画像は、投稿時に自動で縮小されます。
- 画像が見つからない場合は、文章だけ投稿されます。
